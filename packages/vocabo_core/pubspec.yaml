name: vocabo_core
description: Core models, enums, constants, and exceptions for Vocabo
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.11.3

resolution: workspace

dependencies:
  crypto: ^3.0.6
  equatable: ^2.0.7
  json_annotation: ^4.9.0
  talker: ^5.1.16

dev_dependencies:
  test: ^1.25.0
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
